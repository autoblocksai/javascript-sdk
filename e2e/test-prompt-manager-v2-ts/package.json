{
  "name": "test-prompt-manager-v2-ts",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "gen:v2": "prompts generate-v2",
    "gen:v2:watch": "nodemon --quiet --watch prompt-templates --ext \"*\" --exec \"npm run gen:v2\""
  },
  "dependencies": {
    "@autoblocks/client": "file:.yalc/@autoblocks/client",
    "nodemon": "*",
    "ts-jest": "*",
    "typescript": "*",
    "vitest": "^3.0.9"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
